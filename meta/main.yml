---
galaxy_info:
  author: Dmitrii Kashin
  description: install helm application / chart
  role_name: helm_app
  license: MIT

  min_ansible_version: 2.7

  platforms:
    - name: Ubuntu
      versions:
        - bionic

  galaxy_tags:
    - kubernetes
    - k8s
    - helm

dependencies:
  - role: freehck.mkdir
    vars:
      mkdir_directories:
        - "{{ helm_app_cfg_dir }}"
